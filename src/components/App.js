import React from "react";
import { Route, Switch } from "react-router-dom";

const App = () => {
  return (
    <React.Fragment>
      <NavBar />
      <Switch>
        <Route path="/" component={} exact />
      </Switch>
    </React.Fragment>
  );
};
export default App;
